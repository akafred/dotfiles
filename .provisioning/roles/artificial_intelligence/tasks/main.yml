- name: Install AI tools
  homebrew_cask:
    name:
      - chatgpt
      - claude
      - claude-code

- name: Install AI tools
  homebrew:
    name:
      - codex

- name: add entries to .gitignore_global
  lineinfile:
    dest: "{{ ansible_env.HOME }}/.gitignore_global"
    line: "{{ item }}"
  with_items:
    - .claude/
    - .serena/
    - CLAUDE.md
    - AGENTS.md
